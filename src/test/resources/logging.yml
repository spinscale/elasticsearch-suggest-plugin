rootLogger: INFO, console

logger:
  de.spinscale.elasticsearch.service.suggest: DEBUG
  de.spinscale.elasticsearch.action.suggest: DEBUG
  de.spinscale.elasticsearch.action.suggest: DEBUG
  de.spinscale.elasticsearch.service.suggest.SuggestService: DEBUG
  de.spinscale.elasticsearch.rest.action.suggest.SuggestAction: DEBUG
  de.spinscale.elasticsearch.plugin.suggest.SuggestPlugin: DEBUG
  service.suggest: DEBUG
  action.suggest: DEBUG
  action.suggest: DEBUG
  service.suggest.NodeClientWithSuggest: DEBUG
  service.suggest.SuggestService: DEBUG
  rest.action.suggest.SuggestAction: DEBUG
  plugin.suggest.SuggestPlugin: DEBUG

appender:
  console:
    type: console
    layout:
      type: consolePattern
      conversionPattern: "[%d{ISO8601}][%-5p][%-25c] %m%n"
