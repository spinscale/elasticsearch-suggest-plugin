rootLogger: INFO, console

logger:
  service.suggest: TRACE
  org.elasticsearch.action.suggest: TRACE
  action.suggest: TRACE
  service.suggest.NodeClientWithSuggest: TRACE
  service.suggest.SuggestService: TRACE
  rest.action.suggest.SuggestAction: TRACE
  plugin.suggest.SuggestPlugin: TRACE

appender:
  console:
    type: console
    layout:
      type: consolePattern
      conversionPattern: "[%d{ISO8601}][%-5p][%-25c] %m%n"
